# Optional: place a dedicated mainnet RPC endpoint first in the fallback chain.
SOLANA_RPC_URL=

# Server collector cache and client refresh default. Range: 30–3600 seconds.
REPORT_REFRESH_SECONDS=60

# Timeout for each external request. Range: 3000–30000 milliseconds.
REPORT_FETCH_TIMEOUT_MS=12000
